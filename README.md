# diagrams-demo
This repository is a demo of Diagram as Code

## How to generate the diagram

Go to the diagram directory.

```sh
cd docs/quick_start
```

Run the command below.

```sh
poetry run python diagram.py
```

The diagram will be generated in the execution directory.
